# Copy this file to .env and fill in your local PostgreSQL connection details.
# The schema tests use these values to connect; all test data is rolled back
# after each test so nothing persists to the database.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=objectified
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=

